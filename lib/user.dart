import 'package:flutter/material.dart';

class User {
    String email;
    String username;
    String passwd;
    String name;
    String surname;

    User(this.email, this.username, this.name, this.surname, this.passwd);
}